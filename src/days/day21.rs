use days::day16::*;

use std::error::Error;

use std::fs;
use std::path::Path;

pub fn first_star() -> Result<(), Box<Error + 'static>> {
    Ok(())
}

pub fn second_star() -> Result<(), Box<Error + 'static>> {
    Ok(())
}